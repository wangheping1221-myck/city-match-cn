# 开卖清单（国内城市匹配）

## 你现在已经有的

1. `index.html` + `app.js`：可手机打开的测试页  
2. 12 座城结果文案 + 10 道题  
3. 口令门：默认口令 `CITYMATCH`  
4. `小红书文案包.md`：30 条封面金句 + 笔记模板  

## 今天就能做的 4 步

1. **托管网页**  
   - 随便丢到 GitHub Pages / Cloudflare Pages / 任意静态托管  
   - 得到一个可访问链接，例如 `https://xxx.pages.dev`

2. **改口令（可选）**  
   - 打开 `app.js`，改 `ACCESS_CODE`  
   - 演示按钮可先留着，正式卖时可删掉 `previewBtn`

3. **小红书店铺上架**  
   - 虚拟商品，价格 2.9 或 3.9  
   - 自动发货内容写：链接 + 口令  

4. **按文案包发笔记**  
   - 前 7 天每天 3–5 条，只换金句封面  

## 本地预览

用浏览器直接打开 `index.html`，或在本目录运行：

```bash
npx --yes serve .
```

- 测试页：`/`（口令 `CITYMATCH`）
- 封面生成器：`/cover.html`（选金句 → 下载 PNG 或截图）